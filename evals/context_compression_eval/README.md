# context_compression_eval

> :construction: Placeholder. This eval measures the size of the compiled
> context against a baseline that includes the full chat history and every
> retrieved chunk. Implementation follows the harness pattern in
> ``token_savings_eval/run.py``.
