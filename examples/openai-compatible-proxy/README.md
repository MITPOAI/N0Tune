# OpenAI-compatible proxy example

Future example for pointing an OpenAI-compatible client at N0Tune.

No runnable example is implemented in Phase 0 because the proxy endpoint is planned for Phase 3.
